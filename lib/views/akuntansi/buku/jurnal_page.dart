import 'package:flutter/material.dart';

class BukuJurnalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buku Jurnal'),
      ),
      body: Center(
        child: Text('Buku Jurnal Page Content'),
      ),
    );
  }
}

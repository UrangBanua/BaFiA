import 'package:flutter/material.dart';

class JAUmumPage extends StatelessWidget {
  const JAUmumPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jurnal Umum'),
      ),
      body: const Center(
        child: Text('Jurnal Umum masih dalam tahap pengembangan'),
      ),
    );
  }
}

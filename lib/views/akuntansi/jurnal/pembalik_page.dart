import 'package:flutter/material.dart';

class JAPembalikPage extends StatelessWidget {
  const JAPembalikPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jurnal Pembalik'),
      ),
      body: const Center(
        child: Text('Jurnal Pembalik masih dalam tahap pengembangan'),
      ),
    );
  }
}

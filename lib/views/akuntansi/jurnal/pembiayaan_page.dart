import 'package:flutter/material.dart';

class JAPembiayaanPage extends StatelessWidget {
  const JAPembiayaanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jurnal Pembiayaan'),
      ),
      body: const Center(
        child: Text('Jurnal Pembiayaan masih dalam tahap pengembangan'),
      ),
    );
  }
}

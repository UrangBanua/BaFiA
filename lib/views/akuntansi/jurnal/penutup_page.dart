import 'package:flutter/material.dart';

class JAPenutupPage extends StatelessWidget {
  const JAPenutupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jurnal Penutup'),
      ),
      body: const Center(
        child: Text('Jurnal Penutup masih dalam tahap pengembangan'),
      ),
    );
  }
}

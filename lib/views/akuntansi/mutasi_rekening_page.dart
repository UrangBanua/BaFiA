import 'package:flutter/material.dart';

class MutasiRekeningPage extends StatelessWidget {
  const MutasiRekeningPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mutasi Rekening Page'),
      ),
      body: const Center(
        child: Text('Mutasi Rekening Page Content'),
      ),
    );
  }
}

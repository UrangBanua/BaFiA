import 'package:flutter/material.dart';

class JurnalUmumPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Jurnal Umum Page'),
      ),
      body: Center(
        child: Text('Jurnal Umum Page Content'),
      ),
    );
  }
}

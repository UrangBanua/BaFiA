import 'package:flutter/material.dart';

class RBSppPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register Belanja - SPP Page'),
      ),
      body: const Center(
        child: Text('Register Belanja - SPP Page Content'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class RBSppPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register Belanja - SPP Page'),
      ),
      body: Center(
        child: Text('Register Belanja - SPP Page Content'),
      ),
    );
  }
}

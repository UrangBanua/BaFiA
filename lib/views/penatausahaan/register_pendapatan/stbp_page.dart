import 'package:flutter/material.dart';

class RPStbpPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register Pendapatan - STBP'),
      ),
      body: Center(
        child: Text('Register Pendapatan - STBP Page Content'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class LPJAdministratifPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LPJ Administratif Page'),
      ),
      body: Center(
        child: Text('LPJ Administratif Page Content'),
      ),
    );
  }
}

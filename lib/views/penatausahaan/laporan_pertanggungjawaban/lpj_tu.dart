import 'package:flutter/material.dart';

class LPJTUPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('LPJ TU Page'),
      ),
      body: const Center(
        child: Text('LPJ TU Page Content'),
      ),
    );
  }
}
